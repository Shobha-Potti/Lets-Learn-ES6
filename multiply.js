let x, y;
function mul(x, y) { return `The multiplied result is: ${x * y}` }
console.log(mul(12, 3));
let a, b;
let square = function (a) {
    return `The multiplied result is: ${a ** 2}`
}
console.log(square(12));
